export interface Book {
  id: string;
  name: string;
  author: string;
  publisher: string;
  publicationYear: string;
  subject: string;
  createdAt: string;
}

export interface User {
  id: string;
  name: string;
  firstName: string;
  lastName: string;
  email: string;
}
